<h1>Editing Request</h1>

<%= render 'form', request: @request %>

<%= link_to 'Show', @request %>
<% if not current_member %>
	<%= link_to 'Back To Requests', rider_requests_path(@request.rider_id) %>
<% else %>
	<%= link_to 'Back To Requests', requests_path %>
<% end %>

